# Task: Run the project and fix errors

## Steps:
- [x] Remove node-sass from package.json (deprecated, causes build errors)
- [x] Run `npm install` (in progress, TAR warnings expected/non-fatal)
- [x] Fix Node 20 crypto error in webpack by adding --openssl-legacy-provider
- [x] Test dev server at http://localhost:8088 (running successfully)

